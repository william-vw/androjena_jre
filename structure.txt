derivation
	via

nodeSet0 
	hasConclusion
		prov:value	
			rdf:subject, ..

	derivation - recomm
		via nodeSet1
		via nodeSet2
	derivation - recomm
		via nodeSet3
		via nodeSet4 - reason
			derivation
				via nodeSet5
				via nodeSet6
